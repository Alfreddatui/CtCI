// Given a smaller string s and a bigger string b, design an lagorithm to find all permutations of the shorter string within the longer one. Print the location of each permutation.

function printPermutation(s, b) {
  // s.sort((a, b) => {
  //   return (a.attr > b.attr) - (a.attr < b.attr);
  // });
  console.log(typeof s);
}

const s = "abbc";
const b = "cbabadcbbabbcbabaabccbabc";

printPermutation(s, b);